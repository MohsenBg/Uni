// ignore this file
// this file used for extern funtion from c++ to rust

#include "./sorting.h"

// Bubble Sort
//-------------------------------------
extern "C++" short *bubble_sort_i16(short arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" int *bubble_sort_i32(int arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" long *bubble_sort_i64(long arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" unsigned short *bubble_sort_u16(unsigned short arr[],
                                             unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" unsigned *bubble_sort_u32(unsigned arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" unsigned long *bubble_sort_u64(unsigned long arr[],
                                            unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" float *bubble_sort_f32(float arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

extern "C++" double *bubble_sort_f64(double arr[], unsigned size) {
  return sotring::bubble_sort(arr, size);
}

// Insertion Sort
//-------------------------------------
extern "C++" short *insertion_sort_i16(short arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" int *insertion_sort_i32(int arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" long *insertion_sort_i64(long arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" unsigned short *insertion_sort_u16(unsigned short arr[],
                                                unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" unsigned *insertion_sort_u32(unsigned arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" unsigned long *insertion_sort_u64(unsigned long arr[],
                                               unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" float *insertion_sort_f32(float arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

extern "C++" double *insertion_sort_f64(double arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

// Selection Sort
//-------------------------------------
extern "C++" short *selection_sort_i16(short arr[], unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" int *selection_sort_i32(int arr[], unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" long *selection_sort_i64(long arr[], unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" unsigned short *selection_sort_u16(unsigned short arr[],
                                                unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" unsigned *selection_sort_u32(unsigned arr[], unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" unsigned long *selection_sort_u64(unsigned long arr[],
                                               unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" float *selection_sort_f32(float arr[], unsigned size) {
  return sotring::selection_sort(arr, size);
}

extern "C++" double *selection_sort_f64(double arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

// Quick Sort
//-------------------------------------
extern "C++" short *quick_sort_i16(short arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" int *quick_sort_i32(int arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" long *quick_sort_i64(long arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" unsigned short *quick_sort_u16(unsigned short arr[],
                                            unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" unsigned *quick_sort_u32(unsigned arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" unsigned long *quick_sort_u64(unsigned long arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" float *quick_sort_f32(float arr[], unsigned size) {
  return sotring::quick_sort(arr, size);
}

extern "C++" double *quick_sort_f64(double arr[], unsigned size) {
  return sotring::insertion_sort(arr, size);
}

// Merge Sort
//-------------------------------------
extern "C++" short *merge_sort_i16(short arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" int *merge_sort_i32(int arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" long *merge_sort_i64(long arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" unsigned short *merge_sort_u16(unsigned short arr[],
                                            unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" unsigned *merge_sort_u32(unsigned arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" unsigned long *merge_sort_u64(unsigned long arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" float *merge_sort_f32(float arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}

extern "C++" double *merge_sort_f64(double arr[], unsigned size) {
  return sotring::merge_sort(arr, size);
}
